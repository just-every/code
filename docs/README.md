# Documentation Map

- **Core guides:** `getting-started.md`, `install.md`, `faq.md`, `contributing.md`, `config.md`, `exec.md`, `execpolicy.md`, `sandbox.md`, `platform-sandboxing.md`, `release_management.md`, `slash-commands.md`, `advanced.md`.
- **Design/UX:** `tui-card-themes.md`, `tui-chatwidget-refactor.md`, `THEME_CONFIG.md`, `screenshots/`.
- **Workstreams & maintenance:** `maintenance/`, `migration/`, `plans/`, `workstream-c-*`.
- **Auto Drive (active work):** see `working/auto-drive-*` for current state, reliability, QA, and migration tasks.
- **Upstream MCP/ACP (active work):** see `working/upstream-*` for integration and reuse playbooks.
- **Logos/assets:** `logo.png`, `every-logo.png`.
- **Working notes:** `working/` holds in-progress or operational docs; promote or delete once stable.
- **Removed archives:** Old historical docs are deleted to keep the tree lean; recover via git history if needed.
