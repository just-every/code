## @just-every/code v0.6.167

This release adds MCP conformance coverage and improves release publishing stability.

### Changes

- MCP: add conformance regression gates for client behavior and review regressions.
- Release: parallelize R2 asset publishing with DotSlash for faster release uploads.
- CI: grant the blob size policy job read access to repository contents.

### Install

```
npm install -g @just-every/code@latest
code
```

Compare: https://github.com/just-every/code/compare/v0.6.166...v0.6.167
